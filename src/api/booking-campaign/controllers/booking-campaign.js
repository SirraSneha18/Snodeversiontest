'use strict';

/**
 * booking-campaign controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::booking-campaign.booking-campaign');
